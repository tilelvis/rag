# No third-party dependencies — the generator uses only the Python standard library
# (argparse, logging, os, sys, json, random, re, datetime, collections).
# Requires Python 3.12+ (uses PEP 701 f-string syntax and `list[dict]` type hints).
